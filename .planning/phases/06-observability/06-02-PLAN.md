---
phase: 06-observability
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/client.ts
  - src/managers/connection.ts
  - src/transport/websocket.ts
  - src/connection/state.ts
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "Request latency is reported per-method via onRequestLatency"
    - "Connection state changes reported with duration via onConnectionStateChange"
    - "Message throughput reported every second via onMessageThroughput"
    - "All metrics have zero overhead when disabled"
  artifacts:
    - path: "src/client.ts"
      provides: "onRequestLatency emit in request()"
      exports: ["metricsCollector field"]
    - path: "src/connection/state.ts"
      provides: "onConnectionStateChange emit in emit()"
      exports: ["setMetricsCollector method"]
    - path: "src/transport/websocket.ts"
      provides: "onMessageThroughput emit via setInterval"
      exports: ["setMetricsCollector method", "messageCount counter"]
    - path: "src/managers/connection.ts"
      provides: "metricsCollector threading to transport and state machine"
  key_links:
    - from: "src/client.ts"
      to: "src/managers/connection.ts"
      via: "connectionManager.send() -> metricsCollector.onRequestLatency()"
    - from: "src/managers/connection.ts"
      to: "src/transport/websocket.ts"
      via: "transport.setMetricsCollector()"
    - from: "src/managers/connection.ts"
      to: "src/connection/state.ts"
      via: "stateMachine.setMetricsCollector()"
---

<objective>
Wire MetricsCollector into request latency tracking, connection state change reporting, and message throughput counting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/client.ts
@src/managers/connection.ts
@src/transport/websocket.ts
@src/metrics/collector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add latency tracking to client.ts request()</name>
  <files>src/client.ts</files>
  <read_first>
    - src/client.ts (lines 640-712 for request method, lines 100-130 for constructor)
    - src/metrics/collector.ts (MetricsCollector interface)
  </read_first>
  <action>
In `src/client.ts`, add latency tracking to the `request()` method.

1. Store `metricsCollector` in the class (add private field after other managers):
   ```
   private metricsCollector: MetricsCollector | undefined;
   ```

2. In the constructor, accept metricsCollector from config:
   ```
   this.metricsCollector = config.metricsCollector;
   ```

3. In `request()` method, wrap the timing around `this.connectionManager.send(requestFrame)`:
   - Before send: `const requestStartTime = performance.now();`
   - After send: `this.metricsCollector?.onRequestLatency(method, performance.now() - requestStartTime);`

Use the no-op guard pattern: `if (this.metricsCollector) { ... }` to ensure zero overhead when disabled.

Import MetricsCollector type from './metrics/collector.js' at the top of the file.
  </action>
  <verify>
    <automated>grep -n "metricsCollector" src/client.ts && grep -n "onRequestLatency\|performance.now" src/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - client.ts imports MetricsCollector type
    - client.ts has private metricsCollector field
    - Constructor assigns config.metricsCollector to the field
    - request() method calls metricsCollector.onRequestLatency(method, latencyMs) after send
    - No-op guard used: if (this.metricsCollector) check before emit
  </acceptance_criteria>
  <done>Request latency reported via MetricsCollector.onRequestLatency()</done>
</task>

<task type="auto">
  <name>Task 2: Add state change observer to ConnectionStateMachine</name>
  <files>src/connection/state.ts</files>
  <read_first>
    - src/connection/state.ts
    - src/metrics/collector.ts
  </read_first>
  <action>
In `src/connection/state.ts`, add a metrics observer to ConnectionStateMachine.

1. Add private field to store last state timestamp and metrics collector:
   ```
   private lastStateAt: number = Date.now();
   private metricsCollector: MetricsCollector | undefined;
   ```

2. Add `setMetricsCollector` method to register the collector (called by ConnectionManager):
   ```
   setMetricsCollector(collector: MetricsCollector | undefined): void {
     this.metricsCollector = collector;
   }
   ```

3. Modify `emit()` method to report state changes:
   After emitting to listeners, call:
   ```
   if (this.metricsCollector) {
     const durationMs = Date.now() - this.lastStateAt;
     this.metricsCollector.onConnectionStateChange(current, durationMs);
     this.lastStateAt = Date.now();
   }
   ```

Import MetricsCollector type from '../metrics/collector.js'.
  </action>
  <verify>
    <automated>grep -n "metricsCollector\|setMetricsCollector\|onConnectionStateChange" src/connection/state.ts</automated>
  </verify>
  <acceptance_criteria>
    - ConnectionStateMachine has private metricsCollector field
    - setMetricsCollector(collector: MetricsCollector | undefined) method exists
    - emit() calls metricsCollector.onConnectionStateChange(state, durationMs) with duration
    - lastStateAt tracks time spent in each state
  </acceptance_criteria>
  <done>Connection state changes reported via MetricsCollector.onConnectionStateChange()</done>
</task>

<task type="auto">
  <name>Task 3: Add message throughput counter to WebSocketTransport</name>
  <files>src/transport/websocket.ts</files>
  <read_first>
    - src/transport/websocket.ts (lines 230-260 for constructor, lines 410-435 for onmessage handler)
    - src/metrics/collector.ts
  </read_first>
  <action>
In `src/transport/websocket.ts`, add rolling message throughput counter to WebSocketTransport.

1. Add private fields to WebSocketTransport class:
   ```
   private metricsCollector: MetricsCollector | undefined;
   private messageCount: number = 0;
   private throughputInterval: ReturnType<typeof setInterval> | null = null;
   private readonly THROUGHPUT_PERIOD_MS: number = 1000;
   ```

2. Add `setMetricsCollector` method:
   ```
   setMetricsCollector(collector: MetricsCollector | undefined): void {
     this.metricsCollector = collector;
     if (collector && !this.throughputInterval) {
       this.throughputInterval = setInterval(() => {
         if (this.messageCount > 0 && this.metricsCollector) {
           this.metricsCollector.onMessageThroughput(this.messageCount, this.THROUGHPUT_PERIOD_MS);
           this.messageCount = 0;
         }
       }, this.THROUGHPUT_PERIOD_MS);
     }
   }
   ```

3. In `cleanup()` method, add:
   ```
   if (this.throughputInterval) {
     clearInterval(this.throughputInterval);
     this.throughputInterval = null;
   }
   ```

4. In the `ws.onmessage` handler (around line 413), increment counter:
   ```
   this.messageCount++;
   ```

Import MetricsCollector type from '../metrics/collector.js'.
  </action>
  <verify>
    <automated>grep -n "metricsCollector\|messageCount\|throughputInterval\|onMessageThroughput" src/transport/websocket.ts</automated>
  </verify>
  <acceptance_criteria>
    - WebSocketTransport has setMetricsCollector(collector) method
    - Rolling 1-second counter tracks messageCount
    - setInterval emits onMessageThroughput every 1000ms
    - cleanup() clears the interval
    - onmessage handler increments messageCount
  </acceptance_criteria>
  <done>Message throughput reported via MetricsCollector.onMessageThroughput()</done>
</task>

<task type="auto">
  <name>Task 4: Thread metricsCollector from OpenClawClient to all components</name>
  <files>src/managers/connection.ts</files>
  <read_first>
    - src/managers/connection.ts (constructor, how transport is used)
  </read_first>
  <action>
In `src/managers/connection.ts`, wire metricsCollector through ConnectionManager to transport and state machine.

1. Update ConnectionManager constructor to accept metricsCollector:
   ```
   constructor(
     transport: IWebSocketTransport,
     config?: ConnectionManagerConfig,
     reconnectManager?: ReconnectManager,
     authHandler?: AuthHandler,
     metricsCollector?: MetricsCollector
   )
   ```

2. In constructor body, call:
   ```
   if (this.transport && typeof (this.transport as any).setMetricsCollector === 'function') {
     (this.transport as any).setMetricsCollector(metricsCollector);
   }
   ```
   Note: Cast to any to call setMetricsCollector without circular import. The transport is always WebSocketTransport subclass.

3. If stateMachine is passed separately (check if ConnectionManager creates its own state machine), also call `stateMachine.setMetricsCollector(metricsCollector)`.

Import MetricsCollector type from '../metrics/collector.js'.
  </action>
  <verify>
    <automated>grep -n "metricsCollector\|setMetricsCollector" src/managers/connection.ts</automated>
  </verify>
  <acceptance_criteria>
    - ConnectionManager constructor accepts metricsCollector parameter
    - ConnectionManager calls transport.setMetricsCollector(metricsCollector)
    - ConnectionManager calls stateMachine.setMetricsCollector(metricsCollector) if applicable
  </acceptance_criteria>
  <done>ConnectionManager wires metricsCollector to transport and state machine</done>
</task>

</tasks>

<verification>
npm run typecheck passes
npm test -- --run passes
</verification>

<success_criteria>
- Request latency: client.ts request() emits onRequestLatency after each send
- Connection state: ConnectionStateMachine emits onConnectionStateChange on each transition
- Message throughput: WebSocketTransport emits onMessageThroughput every 1 second with count
- Zero overhead when metricsCollector is undefined (no-op guards everywhere)
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability/06-02-SUMMARY.md`
</output>
