---
phase: 06-observability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics/collector.ts
  - src/client-config.ts
  - src/index.ts
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "Consumer can provide MetricsCollector to client config"
    - "SDK emits request latency, connection state changes, and message throughput"
    - "Zero overhead when metricsCollector is not provided"
  artifacts:
    - path: "src/metrics/collector.ts"
      provides: "MetricsCollector callback interface"
      exports: ["MetricsCollector", "isMetricsCollector"]
    - path: "src/client-config.ts"
      provides: "metricsCollector field in ClientConfig"
    - path: "src/index.ts"
      provides: "MetricsCollector type export"
  key_links:
    - from: "src/client-config.ts"
      to: "src/metrics/collector.ts"
      via: "import type MetricsCollector"
    - from: "src/index.ts"
      to: "src/metrics/collector.ts"
      via: "export type { MetricsCollector }"
---

<objective>
Add optional MetricsCollector interface (OBS-01) for request latency, connection state changes, and message throughput. Zero overhead when disabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/client-config.ts
@src/types/logger.ts
@src/index.ts

# Interface to create (new file):
# src/metrics/collector.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetricsCollector interface</name>
  <files>src/metrics/collector.ts</files>
  <read_first>
    - src/types/logger.ts (Logger interface pattern to replicate)
  </read_first>
  <action>
Create `src/metrics/collector.ts` with the `MetricsCollector` callback interface.

The interface has three methods:
1. `onRequestLatency(method: string, latencyMs: number): void` — request latency in milliseconds
2. `onConnectionStateChange(state: string, durationMs?: number): void` — connection state and time in that state
3. `onMessageThroughput(count: number, periodMs: number): void` — message count over period (periodMs=1000)

Pattern from Logger interface (optional hook, no-op guard when absent). Export the interface and a `isMetricsCollector` type guard.
  </action>
  <verify>
    <automated>grep -n "export interface MetricsCollector" src/metrics/collector.ts && grep -n "onRequestLatency\|onConnectionStateChange\|onMessageThroughput" src/metrics/collector.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/metrics/collector.ts exists with exported MetricsCollector interface
    - Interface has: onRequestLatency(method: string, latencyMs: number): void
    - Interface has: onConnectionStateChange(state: string, durationMs?: number): void
    - Interface has: onMessageThroughput(count: number, periodMs: number): void
    - isMetricsCollector type guard function is exported
  </acceptance_criteria>
  <done>MetricsCollector interface created in src/metrics/collector.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add metricsCollector to ClientConfig</name>
  <files>src/client-config.ts</files>
  <read_first>
    - src/client-config.ts
  </read_first>
  <action>
Add `metricsCollector?: MetricsCollector` field to `ClientConfig` interface in `src/client-config.ts`.

Import MetricsCollector type from './metrics/collector.js'. Place it alongside the `logger?: Logger` field (line 81) for consistency.
  </action>
  <verify>
    <automated>grep -n "metricsCollector" src/client-config.ts</automated>
  </verify>
  <acceptance_criteria>
    - ClientConfig interface contains: metricsCollector?: MetricsCollector
    - Import of MetricsCollector type is present
  </acceptance_criteria>
  <done>ClientConfig accepts optional metricsCollector field</done>
</task>

<task type="auto">
  <name>Task 3: Export MetricsCollector from index.ts</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts
  </read_first>
  <action>
Add MetricsCollector exports to `src/index.ts`.

Find the Logger Types section (around line 489-503) and add the MetricsCollector export alongside it. Add:
```
export type { MetricsCollector } from './metrics/collector.js';
```

Keep the existing Logger exports unchanged.
  </action>
  <verify>
    <automated>grep -n "MetricsCollector" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts exports MetricsCollector type
    - Existing Logger exports remain unchanged
  </acceptance_criteria>
  <done>MetricsCollector interface publicly exported from SDK</done>
</task>

</tasks>

<verification>
npm run typecheck passes (no new type errors)
npm test -- --run passes (existing tests still pass)
</verification>

<success_criteria>
- MetricsCollector interface exists in src/metrics/collector.ts with 3 methods
- ClientConfig accepts optional metricsCollector field
- MetricsCollector is exported from src/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability/06-01-SUMMARY.md`
</output>
