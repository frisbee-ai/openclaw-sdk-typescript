# Plan 05-04: JSDoc Security Notes + Already-Complete Items (HARD-04/05/06/07)

**Phase:** 05-hardening
**Wave:** 1
**Requirements addressed:** HARD-04, HARD-05, HARD-06, HARD-07

## Frontmatter

```yaml
requirements_addressed: [HARD-04, HARD-05, HARD-06, HARD-07]
files_modified:
  - src/auth/provider.ts
  - src/connection/tls.ts
depends_on: []
autonomous: true
```

## Context

From `05-CONTEXT.md` (already-implemented items confirmed):

| Requirement | Finding | Status |
|-------------|---------|--------|
| HARD-04 | NodesAPI pairing sub-namespace (`src/api/nodes.ts:125-163`) already fully typed with `Promise<T>` results | ✅ No changes needed |
| HARD-05 | GapDetector gaps array design (`src/events/gap.ts:51`) already uses private array with `reset()` — bounded usage pattern; ring buffer not needed | ✅ No changes needed |
| HARD-06 | `StaticCredentialsProvider` needs JSDoc documenting private key in-memory limitations | 📝 Doc addition only |
| HARD-07 | `TlsValidator` needs JSDoc noting `constantTimeCompare()` timing-safe for equal-length strings | 📝 Doc addition only |

## Tasks

### Task 1: Add Security JSDoc to StaticCredentialsProvider (HARD-06)

<read_first>
- src/auth/provider.ts (full file — especially around StaticCredentialsProvider class and signChallenge method)
</read_first>

<action>
Add a `@security` JSDoc block to `StaticCredentialsProvider` class and/or `signChallenge()` method documenting:

1. On the `StaticCredentialsProvider` class or `privateKey` property:
   ```
   /**
    * @securityNote **Private key held in plaintext in memory.** The private key
    * string is stored in memory after construction. `signChallenge()` wipes
    * the internal keyBuffer after use via crypto.privateEncrypt, but the
    * original `privateKey` string remains in memory. For production use with
    * sensitive keys, consider using an HSM, keyring, or hardware security module.
    * @see https://developer.mozilla.org/en-US/docs/Web/API/SubtleCrypto
    */
   ```

2. The note should be added as a `@securityNote` tag or within the existing JSDoc comment for the class or `signChallenge` method. Do not change any runtime code.

If `StaticCredentialsProvider` already has a `@security` note, verify it covers:
- Private key held in plaintext in memory
- `signChallenge()` wipes keyBuffer but original string remains
- Recommendation for HSM/keyring for production
</action>

<acceptance_criteria>
- [ ] `StaticCredentialsProvider` has a JSDoc `@securityNote` or security-related note
- [ ] Note mentions: private key in plaintext memory, keyBuffer wiped but original string remains
- [ ] Note recommends HSM/keyring for production
- [ ] `npm run build` succeeds (no code changes, only docs)
</acceptance_criteria>

### Task 2: Add Security JSDoc to TlsValidator.constantTimeCompare (HARD-07)

<read_first>
- src/connection/tls.ts (full file — especially around constantTimeCompare method)
</read_first>

<action>
Add a `@security` JSDoc note to `constantTimeCompare()` method documenting:

```
/**
 * @securityNote **Timing-safe comparison for equal-length strings.** Uses XOR loop
 * over the full length of both strings. For strings of equal length, timing
 * is proportional to length rather than to where the first difference occurs,
 * making it resistant to timing attacks. Falls back to lexicographic comparison
 * only when lengths differ (which can leak length difference — acceptable for
 * this use case since unequal lengths already indicate inequality).
 */
```

If a `@securityNote` or similar note already exists, verify it covers:
- XOR loop over full length for timing safety
- Falls back to lexicographic for different-length strings (length difference can be observed)
</action>

<acceptance_criteria>
- [ ] `constantTimeCompare()` has a JSDoc `@securityNote` or security-related note
- [ ] Note mentions: XOR loop, timing-safe for equal-length, fallback for different lengths
- [ ] `npm run build` succeeds (no code changes, only docs)
</acceptance_criteria>

## Verification

```bash
npm run build && npm test -- --run
```

## Must-Haves (Goal-Backward)

1. HARD-04: No code changes — NodesAPI already typed ✓
2. HARD-05: No code changes — GapDetector already bounded ✓
3. HARD-06: StaticCredentialsProvider JSDoc includes security note about plaintext private key
4. HARD-07: TlsValidator.constantTimeCompare JSDoc includes security note about timing safety
