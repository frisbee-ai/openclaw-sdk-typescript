---
phase: 02-code-health
plan: "01"
type: execute
wave: 2
depends_on: ["02-02"]             # ClientBuilder needs types co-located first (REF-02)
files_modified:
  - src/client.ts
  - src/client-builder.ts
  - src/index.ts
autonomous: true
requirements: ["REF-01"]
gap_closure: false

must_haves:
  truths:
    - "ClientBuilder is the primary entry point (per D-03)"
    - "client.ts is reduced to thin facade (≤800 lines)"
    - "All API namespaces are wired through ClientBuilder"
    - "Fluent API works: .withAuth().withReconnect().withGapDetection().withTickMonitor().withLogger().build()"
  artifacts:
    - path: "src/client-builder.ts"
      provides: "Builder pattern entry point with fluent API"
      min_lines: 150
    - path: "src/client.ts"
      provides: "Thin facade delegating to ClientBuilder"
      max_lines: 800
  key_links:
    - from: "src/client-builder.ts"
      to: "src/api/chat.ts, src/api/agents.ts, etc."
      via: "import + instantiation"
    - from: "src/index.ts"
      to: "src/client-builder.ts"
      via: "export ClientBuilder"
---

<objective>
Extract API namespace initialization and connection handler setup from client.ts into ClientBuilder (per REF-01, D-01, D-02, D-03). client.ts becomes a thin facade.

Purpose: Improve maintainability and testability by separating configuration logic from the client facade.
Output: ClientBuilder class with fluent API, reduced client.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/client.ts (1110 lines — TO BE REFACTORED to ≤800)
@src/auth/provider.ts (factory pattern reference: createAuthHandler)
@src/managers/connection.ts (constructor pattern reference)
@src/api/chat.ts, @src/api/agents.ts, @src/api/sessions.ts, etc. (API classes to be wired)
@src/protocol/api-params.ts (types — co-located per REF-02 in parallel plan)
@src/index.ts (public exports — updated after refactor)

## Key Decisions (MUST implement exactly):

**D-01:** Builder pattern — ClientBuilder handles all API wiring, client.ts becomes thin facade
**D-02:** Fluent/chainable API — `.withAuth(token).withReconnect(true).withGapDetection(config).build()`
**D-03:** createClient() is REMOVED — ClientBuilder is the primary entry point (BREAKING CHANGE)

## ClientBuilder API (per D-01, D-02, specifics in 02-CONTEXT.md):

```typescript
// Constructor
new ClientBuilder(url: string, clientId: string, clientVersion?: string)

// Fluent methods (all return this for chaining)
.withAuth(token: string | CredentialsProvider): this
.withReconnect(config: ReconnectConfig | boolean): this
.withGapDetection(config: GapDetectorConfig): this
.withTickMonitor(config: TickMonitorConfig): this
.withLogger(logger: Logger): this

// Build the client
.build(): OpenClawClient
```

## Defaults (from existing DEFAULT_* constants in client.ts):
- requestTimeoutMs: 30000
- connectTimeoutMs: 30000
- maxReconnectAttempts: 10
- reconnectDelayMs: 1000
- autoReconnect: true
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClientBuilder class with fluent API</name>
  <read_first>
    src/client.ts
    src/auth/provider.ts
    src/managers/connection.ts
  </read_first>
  <action>
    Create `src/client-builder.ts` with the following structure:

    1. **Imports:** All managers, API classes, types currently imported in client.ts constructor
    2. **ClientBuilder class:**
       - Private fields: url, clientId, clientVersion, config (all the optional config parts), auth config, reconnect config, tick config, gap config, logger
       - Constructor: `(url: string, clientId: string, clientVersion?: string)` — stores url/clientId
       - `.withAuth(token: string | CredentialsProvider): this` — stores auth
       - `.withReconnect(config: ReconnectConfig | boolean): this` — stores reconnect settings
       - `.withGapDetection(config: GapDetectorConfig): this` — stores gap config
       - `.withTickMonitor(config: TickMonitorConfig): this` — stores tick config
       - `.withLogger(logger: Logger): this` — stores logger
       - `.build(): OpenClawClient` — creates and returns an OpenClawClient instance

    3. **Build logic in .build():**
       - Creates WebSocketTransport
       - Creates AuthHandler if credentials provider configured
       - Creates ReconnectManager
       - Creates ConnectionManager with handlers
       - Calls `setupConnectionHandlers()` (extract from client.ts)
       - Creates RequestManager, EventManager
       - Creates ProtocolNegotiator, PolicyManager, StateMachine
       - Initializes all API namespaces with requestFn closure
       - Returns NEW OpenClawClient instance passing all dependencies

    4. **DO NOT move setupConnectionHandlers() body into ClientBuilder** — keep it in client.ts (called during build). ClientBuilder passes handler factory functions to ConnectionManager.

    5. **Default values:** Use the DEFAULT_* constants from client.ts (requestTimeoutMs=30000, etc.)

    IMPORTANT: OpenClawClient constructor signature stays the SAME (backward compat for tests). ClientBuilder is an ADDITIONAL entry point.
  </action>
  <verify>
    <automated>grep -c "class ClientBuilder" src/client-builder.ts && grep -c "withAuth\|withReconnect\|withGapDetection\|withTickMonitor\|withLogger" src/client-builder.ts</automated>
  </verify>
  <done>ClientBuilder class exists with fluent API matching D-01, D-02, D-03</done>
  <acceptance_criteria>
    - ClientBuilder class is exported from src/client-builder.ts
    - Constructor takes (url, clientId, clientVersion?)
    - withAuth(), withReconnect(), withGapDetection(), withTickMonitor(), withLogger() all return `this`
    - build() returns OpenClawClient
    - All DEFAULT_* values from client.ts are used as defaults
    - grep finds "class ClientBuilder" in the file
    - grep finds at least 5 fluent method names in the file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refactor client.ts into thin facade</name>
  <read_first>
    src/client.ts
    src/client-builder.ts
  </read_first>
  <action>
    Transform client.ts into a thin facade:

    1. **Remove from client.ts:**
       - DEFAULT_* constants (move to client-builder.ts or keep if used elsewhere)
       - ConnectionConfig interface (move to client-builder.ts)
       - ClientConfig interface (move to client-builder.ts)
       - All private manager instances (connectionManager, requestManager, eventManager, etc.)
       - API namespace private fields (_chatAPI, _agentsAPI, etc.)
       - Constructor body (all the initialization logic)
       - setupConnectionHandlers() method body
       - normalizeConnectionConfig() method
       - performSnapshotRecovery() method
       - generateRequestId() method
       - All @example blocks in getters (simplify)

    2. **Keep in client.ts:**
       - All public getters: isConnected, connectionState, protocol, serverInfo, snapshot, isReady
       - All public methods: connect(), disconnect(), request(), abort()
       - All event subscription methods: onStateChange(), onError(), onMessage(), onClosed(), on(), once(), off(), listenerCount()
       - All private fields that are PASSED IN or computed from passed-in state (not managers)
       - The tickMonitor and gapDetector private fields (set during connect())

    3. **Simplify constructor:**
       - Only stores: _config, _normalizedConfig, logger
       - Sets up nothing else (delegates to ClientBuilder internally if needed)

    4. **ClientBuilder integration:**
       - OpenClawClient constructor now accepts an OPTIONAL pre-built set of managers
       - OR: Keep OpenClawClient constructor as-is but have ClientBuilder.build() call a static factory that creates OpenClawClient with all deps

    5. **CONFLICT with D-03 (createClient removed):**
       - Per D-03: createClient() is removed and ClientBuilder is the primary entry
       - HOWEVER: OpenClawClient must still be constructible for backward compat
       - Solution: Keep OpenClawClient constructor working (accepts ClientConfig as before) but have ClientBuilder.build() call it
       - CreateClientFactory is INTERNAL to ClientBuilder.build()
       - Export ClientBuilder from index.ts, remove createClient export

    6. **Line count target:** client.ts must be ≤800 lines after refactor
  </action>
  <verify>
    <automated>wc -l src/client.ts | awk '{print $1}' && grep -c "constructor" src/client.ts</automated>
  </verify>
  <done>client.ts reduced to thin facade, constructor simplified, all manager init moved to ClientBuilder</done>
  <acceptance_criteria>
    - client.ts line count is ≤ 800 (measured via `wc -l`)
    - OpenClawClient constructor still exists and is backward-compatible
    - All public methods (connect, disconnect, request, abort, on*, once, off, listenerCount) still present
    - All public getters (isConnected, connectionState, protocol, etc.) still present
    - No API namespace initialization code in client.ts constructor
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update index.ts exports</name>
  <read_first>
    src/index.ts
    src/client-builder.ts
    src/client.ts
  </read_first>
  <action>
    Update src/index.ts:

    1. **Remove export:**
       - `createClient` factory function (per D-03 — BREAKING CHANGE)

    2. **Add export:**
       - `ClientBuilder` class from src/client-builder.ts

    3. **Keep:**
       - OpenClawClient class export (thin facade)
       - All other existing exports (errors, managers, events, etc.)

    4. **Update export comment** for ClientBuilder to document fluent API usage

    5. **Verify no other changes needed** to index.ts for API types — those are handled by REF-02 plan
  </action>
  <verify>
    <automated>grep -c "ClientBuilder" src/index.ts && ! grep -c "createClient" src/index.ts</automated>
  </verify>
  <done>index.ts exports ClientBuilder, createClient removed per D-03</done>
  <acceptance_criteria>
    - ClientBuilder is exported from index.ts
    - createClient is NOT exported from index.ts (grep confirms absence)
    - All other existing exports remain intact
    - TypeScript compiles without errors (npm run typecheck passes)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks:
1. Run `npm run typecheck` — must pass
2. Run `npm test` — all tests must pass (backward compat)
3. Verify client.ts line count: `wc -l src/client.ts` shows ≤ 800 lines
4. Verify ClientBuilder is exported: `grep "export.*ClientBuilder" src/index.ts`
5. Verify createClient is removed: `grep "createClient" src/index.ts` returns nothing
</verification>

<success_criteria>
- client.ts is reduced to ≤800 lines (measured)
- ClientBuilder handles all API wiring (confirmed via grep)
- Fluent API: .withAuth().withReconnect().withGapDetection().withTickMonitor().withLogger().build() works
- createClient() is removed from exports (D-03)
- All existing tests pass (backward compatible OpenClawClient constructor)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-health/02-01-SUMMARY.md`
</output>
